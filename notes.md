1. dorin's comments:

    - ~~declare types of arguments #red~~s
    - ~~set the declaration of the short string inside the "get_string_from_short_string" func #red~~
    - ~~delete none necessary importing #red~~
    - ~~create a requirements file instead of setup ???? #red~~
    - ~~fix the files inside the project and inside github #red~~
    - ~~ctrl+alt+L to set my python file by the Python's official style \
      guide way. and it to set the lines in correct spaces format ,* OR fix it one by one #red~~
    - ~~rename the get_string_from_shortstring to something else like "translate" #red~~
    - ~~change the mentioning set_short_string function apearing in the code to straight up be : \
      " shortstring = get_string_from_shortstring(Enum.string)" #red~~
    - ~~erase set_string_to_short_string #red~~
    - ~~do not have any empty lines between "if's" "else's" and such #red~~
    - ~~make all the constants from get .env be from constants, and uppercase the constant #red~~
    - ~~try to find an automatic way to change the format of python (not EPE 8) , and check what the format is called #red~~
    - ~~erase all unnecessary "()" #red~~
    - ~~erase setup #red~~
    - ~~change Enum class names~~
    - ~~move classes to their own file - they are not constants~~
    - ~~hange subscriber1 and publisher1 (in main), to another appropriate name~~
    - ~~check if I can import only the 2 pairs of functions from sub and pub (running and constructor)~~
    - ~~declare specific types for pubs and detections~~
    - ~~declaring ENUM types~~
    - ~~rename Enums better~~
    - remove my short string conversion functions and implement the regular local "encode/decode("utf-8")" type functions


    



